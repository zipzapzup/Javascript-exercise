const num1 = 100;
const num2 = 200;

let val;

// Simple Math with Numbers

val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 * num2;

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.4);
val = Math.round(2.8);
val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.sqrt(64);
val = Math.abs(-3); // Absolute Value
val = Math.pow(8,2);
val = Math.min(2,33,4,1,55,6,3);
val = Math.max(2,33,4,1,55,6,3);
val = Math.random(); // Math Random will generate random value between 0 and 1

val = Math.floor(Math.random() * 20); // This will get you random value between 0-19

console.log(val);